#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../../../.17.0.3@jest-cli/bin/jest.js" "$@"
  ret=$?
else 
  node  "$basedir/../../../.17.0.3@jest-cli/bin/jest.js" "$@"
  ret=$?
fi
exit $ret
